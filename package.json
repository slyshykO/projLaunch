{
  "name": "projlaunch",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "prebuild": "dotnet tool restore && dotnet paket restore",
    "dev": "dotnet tool restore && dotnet fable watch src -o ./src/bin/fable-js -e .js --sourceMaps --configuration Debug --run vite --mode development",
    "start": "dotnet tool restore && dotnet fable watch src -o ./src/bin/fable-js --run vite serve",
    "build": "dotnet tool restore && dotnet fable src -o ./src/bin/fable-js --run vite build --emptyOutDir",
    "build:serde-probe": "pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/build-serde-probe.ps1",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.3",
    "@tauri-apps/plugin-window-state": "^2.3.0",
    "daisyui": "^5.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^4.0"
  },
  "devDependencies": {
    "@glutinum/cli": "^0.13.0",
    "@tailwindcss/vite": "=4.2.2",
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^6.0.0",
    "glob": "^13.0.0",
    "vite": "^8.0",
    "vite-plugin-compression2": "^2.5.0",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
